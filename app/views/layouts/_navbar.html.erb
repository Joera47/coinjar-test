<div class="main-navbar d-flex align-items-center shadow-sm">
  <%= link_to "", class: "flex-grow-1 text-primary" do %>
    <h5 class="mb-0 font-weight-bold">Coinjar Test</h5>
  <% end %>

  <%= link_to latest_prices_path, class: "#{action_name == "latest" ? "text-info" : "text-secondary" } ml-4" do %>
    <h5 class="mb-0 font-weight-bold">Latest Prices</h5>
  <% end %>

  <%= link_to prices_path, class: "#{action_name == "index" ? "text-info" : "text-secondary" } mx-4" do %>
    <h5 class="mb-0 font-weight-bold">History</h5>
  <% end %>
</div>

<div class="navbar-padding"></div>

<style>
  .main-navbar {
    height: 65px;
    position: fixed;
    background-color: white;
    padding: 10px 30px;
    width: 100%;
    top: 0;
    left: 0;
  }

  .navbar-padding {
    padding-top: 65px;
  }

  .navbar-navigation {
    color: #555;
  }
</style>